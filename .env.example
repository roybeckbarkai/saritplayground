# Email settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASSWORD=your_app_password_here
EMAIL_TO=sera@email.com

# Optional: Claude API for classifying ambiguous job titles
ANTHROPIC_API_KEY=sk-ant-...

# SQLite dedup database path (default: ~/.job_scraper/seen_jobs.db)
# DB_PATH=/path/to/seen_jobs.db

# Set to 1 to print email HTML to stdout instead of sending (for testing)
# DRY_RUN=1
